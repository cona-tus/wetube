extends base.pug
include mixins/video

block navigation
    nav
        ul
            li
                a(href="/videos/upload") Upload Video

block content
    each item in videos
        +video(item)
    else 
        li Sorry nothing found.
